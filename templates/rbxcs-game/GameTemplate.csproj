<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Library</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- The single reference that provides typed C#, IntelliSense, and `.luau` emission on build. -->
  <ItemGroup>
    <PackageReference Include="RobloxCS.Sdk" Version="0.1.0-alpha" />
  </ItemGroup>

</Project>
